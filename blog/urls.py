from django.urls import path
# from .views import PostListView, PostDetailView

app_name = 'blog'

urlpatterns = [
    # path('api/', PostListView.as_view(), name='postlist'),
    # path('api/<int:pk>/', PostDetailView.as_view(), name='postdetail'),
]
